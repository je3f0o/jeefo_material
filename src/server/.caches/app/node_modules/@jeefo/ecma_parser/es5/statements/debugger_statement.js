 jeefo.register("node_modules/@jeefo/ecma_parser/es5/statements/debugger_statement.js", async (exports, module) => { const __dirname = "node_modules/@jeefo/ecma_parser/es5/statements", __filename = "node_modules/@jeefo/ecma_parser/es5/statements/debugger_statement.js"; const require = path => { return jeefo.require(path, __filename, __dirname); }; /* -.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.
* File Name   : debugger_statement.js
* Created at  : 2019-03-01
* Updated at  : 2020-06-11
* Author      : jeefo
* Purpose     :
* Description :
* Reference   :
.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.-.*/
// ignore:start
"use strict";

/* globals*/
/* exported*/

// ignore:end

const { statement }           = (await require("../enums/states_enum"));
const { STATEMENT }           = (await require("../enums/precedence_enum"));
const { is_terminator }       = (await require("../../helpers"));
const { terminal_definition } = (await require("../../common"));

module.exports = {
	id         : "Debugger statement",
	type       : "Statement",
	precedence : STATEMENT,

    is         : (_, { current_state: s }) => s === statement,
    initialize : (node, current_token, parser) => {
        const keyword = terminal_definition.generate_new_node(parser);

        let terminator = null;
        parser.prepare_next_state();
        if (is_terminator(parser)) {
            terminator = terminal_definition.generate_new_node(parser);
        }

        node.keyword    = keyword;
        node.terminator = terminator;
        node.start      = keyword.start;
        node.end        = (terminator || keyword).end;

        parser.terminate(node);
    }
};
 }); 
//# sourceURL=node_modules/@jeefo/ecma_parser/es5/statements/debugger_statement.js 